/**
 * Site configuration
 *
 * SITE_LIVE — controls which home page visitors see:
 *   false → shows the temporary landing page (current)
 *   true  → shows the full site with scattered images gallery
 */
export const SITE_LIVE = false
